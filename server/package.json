{
  "name": "restroos-server",
  "version": "0.1.0",
  "description": "Backend API for RestroOS - multi-tenant restaurant ordering/POS platform",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "vitest run",
    "migrate": "node -e \"import('./src/config/migrate.js').then(m => m.runMigrations()).then(() => { console.log('Migrations applied.'); process.exit(0); }).catch(err => { console.error(err); process.exit(1); })\""
  },
  "engines": {
    "node": ">=22"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/functions": "^3.7.6",
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.10.0",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "pg": "^8.22.0",
    "pusher": "^5.3.4",
    "razorpay": "^2.9.8",
    "resend": "^6.18.0",
    "twilio": "^6.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "vitest": "^4.1.10"
  }
}
